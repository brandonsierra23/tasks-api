export enum EModules {
  tasks = 'tasks',
}

export enum metadata {
  MetaModules = 'module-protected',
  MetaPermission = 'permissions-protected',
}

export enum RestApiRequest {
  get = 'Get',
  post = 'Post',
  patch = 'Patch',
  delete = 'Delete',
}
